<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/spaces"
    android:gravity="center"
    android:padding="16dp">

    <!-- Thank You Message -->
    <TextView
        android:id="@+id/thankYouTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tack!"
        android:textColor="@color/white"
        android:textSize="40sp"
        android:textStyle="bold"
        android:gravity="center"
        android:padding="20dp" />

    <!-- Description Text -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Din bokningsförfrågan har skickats till administratören. Du kommer att få ett bekräftelsesvar inom kort."
        android:textSize="18sp"
        android:textColor="@color/white"
        android:gravity="center"
        android:padding="16dp" />

    <!-- Buttons for Exit and Re-book -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="40dp">

        <!-- Re-book Button -->
        <Button
            android:id="@+id/rebookButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Boka om"
            android:backgroundTint="#8ED567"
            android:layout_marginEnd="24dp"
            android:minHeight="48dp"
            android:padding="8dp" />

        <!-- Exit Button -->
        <Button
            android:id="@+id/exitButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Utgång"
            android:minHeight="48dp"
            android:backgroundTint="#8ED567"
            android:padding="8dp" />
    </LinearLayout>
</LinearLayout>
